<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MyFinanceApp.ViewModels"
             x:Class="MyFinanceApp.Views.TransactionsPage"
             Title="Transactions">
  <ContentPage.BindingContext>
    <vm:TransactionsViewModel />
  </ContentPage.BindingContext>
  <VerticalStackLayout Padding="12">
    <Button Text="Load" Command="{Binding LoadAsyncCommand}" />
    <Button Text="Add Sample" Command="{Binding AddSampleAsyncCommand}" />
    <Button Text="Sync" Command="{Binding SyncAsyncCommand}" />
    <CollectionView ItemsSource="{Binding Transactions}">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Grid Padding="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackLayout>
              <Label Text="{Binding Title}" FontAttributes="Bold" />
              <Label Text="{Binding Category}" FontSize="12" />
            </StackLayout>
            <Label Grid.Column="1" Text="{Binding Amount}" VerticalOptions="Center" />
          </Grid>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>
  </VerticalStackLayout>
</ContentPage>
